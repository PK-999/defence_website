analytics:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: '../../prisma/dev.db'
      schema_directory: '../../prisma'
