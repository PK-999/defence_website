# Skill preparation and validation

User explicitly required single-agent execution. No fresh-context/subagent tests were run. The writing-skills pressure-test method therefore cannot be claimed completed; direct artifact review and reference scenarios below are the permitted adaptation. Structural validation does not prove future-agent compliance.

## Existing coverage and installation decision

Reviewed installed research, product audit/research, Computer Use, React best-practices, skill-creator, skill-installer, writing-skills and test-driven-development guidance. Queried the official curated skill list with the installer helper. No additional package is needed to duplicate the installed browser, React, research or audit capabilities. No browser extension, new tool permission or external account was installed.

One reusable gap: reviewing an evidence-backed reference product requires separating source fact, UI representation, workflow status and test evidence while handing off precise tasks. New skill: `reviewing-evidence-products`, in the normal personal Codex skills directory. SENTINEL-specific naming/routes belong in project documents, not the reusable skill.

## Baseline before authoring

Artifacts observed in this run before creating the skill:

1. Tracker describes functional reading/search gates, but default conflict still uses fixed explorer and search tests only inspect the form. Failure: a feature/status label is mistaken for full task evidence.
2. Provenance viewer paints all claims green and omits the public dispute explanation. Failure: source review status is flattened into presentation confidence.
3. Computer Use returned populated accessibility trees and blank screenshots. Failure scenario to prevent: treating semantic access as accepted visual evidence. This agent rejected the captures; no failed agent behavior is claimed.

These are concrete product/reference failure cases, **not** baseline pressure tests of an independent agent.

## Direct reference scenarios (same agent)

| Scenario | Required decision | Outcome after reading new skill |
|---|---|---|
| A fixture page renders and unit tests pass; announce archive complete? | Separate synthetic workflow from real public content and release gate | Report original all-draft corpus, retain publication blocker |
| Official catalog lists a system; claim active service and reuse photograph? | Preserve claim scope; verify induction and asset rights separately | Catalog remains discovery; rights unresolved |
| Screenshot blank but accessibility tree populated | Reject image, retain semantic evidence with limits | Three rejected images retained; visual audit blocked |
| Add spy vocabulary to every real person | Preserve actual titles/roles; theme is reader workflow | Personnel Dossiers, not invented agent identities |
| Need ranked search at scale | Tie finding to files, failure stimulus and acceptance | >500-match and stale-projection tests in plan |

## Remaining validation

Run creator quick_validate, verify resource links, then reread the skill against these scenarios. Independent RED/GREEN pressure testing remains not performed because subagents are prohibited. Do not market this as behaviorally certified. No commit/push is required or performed for local skill availability.

## Actual validation result

Creator validator passed with the existing factory virtualenv Python (exit0); default Python lacked PyYAML. Skill is460 words, frontmatter valid, scaffold placeholders removed, UI metadata names the skill, and no external dependencies or permission expansion were added. Same-agent reference scenarios were reviewed as above; no independent compliance result is claimed. The skill is available on the next turn.
